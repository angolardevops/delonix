# `delonix rmi`

> Remove an image (or one of its tags)

```text
Remove an image (or one of its tags)

Usage: delonix rmi [OPTIONS] <IMAGE>

Arguments:
  <IMAGE>  

Options:
      --pt-pt  Show help in Portuguese (pt-PT) instead of English
  -h, --help   Print help
```
