# `delonix start`

> (Re)start one or more stopped containers — used by the generated systemd units

```text
(Re)start one or more stopped containers — used by the generated systemd units

Usage: delonix start [OPTIONS] <CONTAINERS>...

Arguments:
  <CONTAINERS>...  

Options:
      --pt-pt  Show help in Portuguese (pt-PT) instead of English
  -h, --help   Print help
```
