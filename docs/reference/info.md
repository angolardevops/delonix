# `delonix info`

> Show engine info + the aggregate resource budget (host protection)

```text
Show engine info + the aggregate resource budget (host protection)

Usage: delonix info [OPTIONS]

Options:
      --pt-pt  Show help in Portuguese (pt-PT) instead of English
  -h, --help   Print help
```
