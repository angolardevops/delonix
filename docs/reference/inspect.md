# `delonix inspect`

> Show low-level JSON for a container or image

```text
Show low-level JSON for a container or image

Usage: delonix inspect [OPTIONS] <TARGET>

Arguments:
  <TARGET>  

Options:
      --pt-pt  Show help in Portuguese (pt-PT) instead of English
  -h, --help   Print help
```
