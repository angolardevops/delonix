# `delonix healthcheck`

> Run the image's HEALTHCHECK command in a container (healthy/unhealthy)

```text
Run the image's HEALTHCHECK command in a container (healthy/unhealthy)

Usage: delonix healthcheck [OPTIONS] <CONTAINER>

Arguments:
  <CONTAINER>  

Options:
      --pt-pt  Show help in Portuguese (pt-PT) instead of English
  -h, --help   Print help
```
